$('#some-div').append(
  '<div class="note">' +
    '<h3>' + data.title + '</h3>' +
    '<p>' + data.details + '</p>' +
  '</div>'
  `<div class="note"> 
    <h3>${data.title}</h3> 
    <p>${data.details}</p> 
  </div>`
);

// data.title